steps:
  - task: UseDotNet@2
    inputs:
      version: $(dotNetVersion)
      includePreviewVersions: true

  - task: DotNetCoreCLI@2
    name: "Build-API"
    inputs:
      command: 'build'
      projects: '**/Quizify.Api.App.csproj'